# Stage/*エリア 色統一の確認ページリスト

## 📋 確認すべきページ（優先度順）

### 🔴 高優先度（必須確認）

#### 1. `/stage` - Stage Top Page
**変更ファイル:**
- `apps/studio.giselles.ai/app/stage/(top)/form.tsx`
- `apps/studio.giselles.ai/app/stage/(top)/settings-dialog.tsx`
- `apps/studio.giselles.ai/app/stage/(top)/page.tsx`
- `apps/studio.giselles.ai/app/stage/(top)/circular-carousel.tsx`

**変更内容:**
- `text-white-400` → `text-inverse`（アイコン、テキスト）
- `text-black-600` → `text-text/60`（日時・メタ情報）

**確認ポイント:**
- [ ] 設定アイコン（Settings）が `text-inverse` で表示されているか
- [ ] アプリが無い場合のメッセージが `text-inverse` で表示されているか
- [ ] アプリラベルが `text-inverse` で表示されているか
- [ ] 閉じるボタンのアイコンが `text-inverse` で表示されているか
- [ ] View Style設定ダイアログが正しく表示されるか
  - [ ] タイトルが `text-inverse` で表示されているか
  - [ ] Closeボタンが `text-inverse` で表示されているか
  - [ ] Display Typeラベルが `text-inverse` で表示されているか
  - [ ] List/Carouselオプションのタイトルが `text-inverse` で表示されているか
  - [ ] 説明文が `text-text-muted` で表示されているか
  - [ ] Fontセクションのラベルが `text-inverse` で表示されているか
- [ ] 空の状態メッセージ（Carousel）が `text-inverse` で表示されているか
- [ ] 実行履歴の日時表示が `text-text/60` で表示されているか

#### 2. `/stage/showcase` - Showcase Page
**変更ファイル:**
- `apps/studio.giselles.ai/app/stage/showcase/showcase-client.tsx`

**変更内容:**
- `text-black-400` → `text-text-muted`（空の状態メッセージ、プレースホルダー）

**確認ポイント:**
- [ ] Appsタブでアプリが無い場合のメッセージが `text-text-muted` で表示されているか
  - [ ] 検索結果が無い場合のメッセージ
  - [ ] アプリがまだ無い場合のメッセージ
- [ ] Playlistタブでプレイリストが無い場合のメッセージが `text-text-muted` で表示されているか
- [ ] Historyタブで履歴が無い場合のメッセージが `text-text-muted` で表示されているか
  - [ ] 検索結果が無い場合のメッセージ
  - [ ] 履歴がまだ無い場合のメッセージ
- [ ] New Playlist Detailsダイアログのプレースホルダーが `text-text-muted` で表示されているか
  - [ ] Playlist title入力欄
  - [ ] Playlist description入力欄

#### 3. `/stage/acts` - Tasks Page
**変更ファイル:**
- `apps/studio.giselles.ai/app/stage/acts/components/filterable-acts-list.tsx`
- `apps/studio.giselles.ai/app/stage/acts/[actId]/ui/sidebar.tsx`

**変更内容:**
- `text-black-400` → `text-text-muted`（説明文）
- `text-white-600` → `text-text/60`（検索アイコン）
- `text-black-600` → `text-text/60`（日時・メタ情報）
- `text-white-400` → `text-inverse`（チーム名）

**確認ポイント:**
- [ ] Tasksページの説明文が `text-text-muted` で表示されているか
- [ ] 検索アイコンが `text-text/60` で表示されているか
- [ ] 実行履歴の日時・チーム名が `text-text/60` で表示されているか
- [ ] タスク詳細サイドバーのチーム名が `text-inverse` で表示されているか

### 🟡 中優先度（視覚確認推奨）

#### 4. Workflow Designer - Text Generation Node
**変更ファイル:**
- `internal-packages/workflow-designer-ui/src/editor/properties-panel/text-generation-node-properties-panel/generation-panel.tsx`
- `internal-packages/workflow-designer-ui/src/editor/properties-panel/text-generation-node-properties-panel/outputs/components.tsx`

**変更内容:**
- `text-black-400` → `text-text-muted`（空の状態、使用量情報、ラベル）
- `text-white-400` → `text-inverse`（Toggle項目）
- `text-white-800` → `text-inverse`（ヘッダーテキスト）
- `text-black-900` → `text-bg`（Updateボタン）
- `hover:text-black-300` → `hover:text-text/60`（Clipboardボタン）
- `hover:border-white-800` → `hover:border-inverse`（Select Sourcesボタン）

**確認ポイント:**
- [ ] テキスト生成ノードのプロパティパネルを開く
- [ ] まだ生成されていない場合の空の状態メッセージが `text-text-muted` で表示されているか
- [ ] 生成完了後の使用量情報（実行時間、トークン数）が `text-text-muted` で表示されているか
  - [ ] Timerアイコン
  - [ ] ArrowUpアイコン（入力トークン）
  - [ ] ArrowDownアイコン（出力トークン）
- [ ] クリップボードボタンが `text-text-muted` で表示され、ホバー時に `text-text/60` になるか
- [ ] OutputsセクションのSelect Sourcesボタンが `text-inverse` で表示され、ホバー時に `border-inverse` になるか
- [ ] Outputsセクションのカテゴリラベル（Action, Generated Content, Text, File, GitHub, Query）が `text-text-muted` で表示されているか
- [ ] OutputsセクションのToggle項目が `text-inverse` で表示されているか
- [ ] Updateボタンが `text-bg` で表示されているか

## 🎨 色の統一マッピング

| 旧トークン | 新トークン | 使用箇所 |
|----------|----------|---------|
| `text-white-400` | `text-inverse` | 主要テキスト、アイコン |
| `text-black-400` | `text-text-muted` | 説明文、補助テキスト |
| `text-white-800` | `text-inverse` | 強調テキスト |
| `text-black-600` | `text-text/60` | 日時、メタ情報、アイコン |
| `text-white-600` | `text-text/60` | アイコン |
| `text-black-900` | `text-bg` | ボタンテキスト（背景が `bg-bg` の場合） |
| `text-black-300` | `text-text/60` | ホバー状態 |
| `hover:border-white-800` | `hover:border-inverse` | ボーダーホバー |

## 📝 確認時の注意事項

1. **ダイアログの確認**
   - View Style設定ダイアログは右上の設定アイコンから開く
   - New Playlist Detailsダイアログは「New Playlist +」ボタンから開く

2. **空の状態の確認**
   - アプリが無い状態で確認する
   - 検索結果が無い状態で確認する
   - 履歴が無い状態で確認する

3. **ホバー状態の確認**
   - クリップボードボタンのホバー
   - Select Sourcesボタンのホバー（ボーダーが変わる）

4. **Workflow Designerの確認**
   - テキスト生成ノードを作成してプロパティパネルを開く
   - 生成前と生成後の両方の状態を確認する

