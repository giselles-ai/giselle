# 次のタスク - 現在の状態と優先順位

## ✅ 完了したタスク

### 1. `bg-[color-mix(...)]`が効かない問題の解決 ✅
**完了:**
- ✅ `_5%`構文（アンダースコア）で動作することが確認された
- ✅ Tailwindの任意値構文ではスペースをアンダースコアに置き換える必要がある
- ✅ `in_srgb`と`_5%`構文に統一完了

### 2. `border-inverse`の置換 ✅
**完了:**
- ✅ `border-inverse`を`border-[var(--color-text-inverse, #fff)]`に置換（2箇所）
- ✅ `border-inverse/20`を`border-[color-mix(in_srgb,var(--color-text-inverse,#fff)_20%,transparent)]`に置換（6箇所）
- ✅ `aliases.css`から`border-inverse`定義を削除
- ✅ 8ファイルを更新

## 🟠 高優先度

### 3. `aliases.css`の収束継続
**現状:**
- ✅ `text-link-muted`の置換完了・削除済み
- ✅ `text-bg`の置換完了・削除済み
- ✅ `bg-inverse`の置換完了・削除済み
- ✅ `border-inverse`の置換完了・削除済み

**残っている主要な定義:**
- `text-inverse` - 多数の箇所で使用中（広範囲）
- `text-text` - 多数の箇所で使用中（広範囲）
- `text-accent`, `bg-accent`, `border-accent` - 使用中
- `text-secondary`, `bg-secondary` - 使用中
- `placeholder:text-inverse/30`, `placeholder:text-inverse/40` - 使用中（4ファイル）
- `hover:text-text/*`, `hover:text-inverse/*` - 使用中
- `text-text/*` (opacity variants) - 使用中
- `bg-surface`, `bg-bg`, `bg-transparent`, `bg-stage`, `bg-auth` - 使用中

**TODO:**
- [ ] `placeholder:text-inverse/30`と`placeholder:text-inverse/40`の置換（4ファイル）
- [ ] その他の`aliases.css`定義の使用状況調査
- [ ] 段階的な置換と削除（優先度の低いものから）


## 🟡 中優先度

### 4. `placeholder:text-inverse/30`と`placeholder:text-inverse/40`の置換
**現状:**
- `placeholder:text-inverse/30`または`placeholder:text-inverse/40`が4ファイルで使用中
- `aliases.css`で定義されているが、トークン直参照へ置換が必要

**TODO:**
- [ ] 使用箇所の確認
- [ ] `placeholder:text-[color-mix(...)]`への置換
- [ ] `aliases.css`から定義を削除（必要に応じて）

**影響ファイル:**
- `field.tsx` (settings/components)
- `anthropic-web-search.tsx` (workflow-designer-ui)
- `chat-panel.tsx` (workflow-designer-ui)
- その他

### 5. 安全置換の実行
**現状:**
- `text-white-*/text-black-*`の置換完了（100%）
- `focus:ring-white/20`、`focus:ring-white/30`の置換完了
- `ring-white/10`の置換完了

**TODO:**
- [ ] codemod safe-pass の実行（dry-run完了、0件）
- [ ] 安全置換の第1弾実行（視覚差なし基準）
- [ ] 生色使用の継続削減（rgba/rgb/hex のトークン化）

## 🟢 低優先度

### 6. scopes の実装
**現状:**
- scopes の CSS ファイルは雛形のみ（空の状態）
- `data-scope`属性の使用が未実装

**TODO:**
- [ ] settings-apps スコープの実装と適用
- [ ] workspaces スコープの実装と適用
- [ ] stage スコープの実装と適用
- [ ] `data-scope`属性の追加と動作確認

### 7. コンポーネントの逆色対応
**現状:**
- `Separator`, `Tabs`は`variant="inverse"`対応済み
- `InverseSurface`は実装済みだが適用状況不明

**TODO:**
- [ ] `Switch/Slider`の逆色既定対応（未実装）
- [ ] `InverseSurface`の適用状況確認と拡大
- [ ] 逆色対応コンポーネントの統一確認

### 8. v3ブリッジ削除計画
**現状:**
- `aliases.css`の収束が進行中
- 互換トークン（`white-900`等）が残っている

**TODO:**
- [ ] v3ブリッジ段階削除計画の起票
- [ ] 互換トークン段階削除（`white-900`等）
- [ ] lintのwarn→errorへの格上げ（stylelint/ESLint）
- [ ] CI で生色/alias の残件を検出 → 増加fail化

## 📊 現在の統計

### `bg-[color-mix(...)]`の使用状況
- **総数:** 67箇所（44ファイル）
- **構文:** `in_srgb`と`_5%`（アンダースコア）構文に統一完了 ✅

### `border-inverse`の使用状況
- **総数:** 0箇所 ✅ 置換完了

### `aliases.css`の定義状況
- `text-link-muted`: ✅ 削除済み
- `text-bg`: ✅ 削除済み
- `bg-inverse`: ✅ 削除済み
- `border-inverse`: ✅ 削除済み
- `placeholder:text-inverse/30`, `placeholder:text-inverse/40`: ❌ 残っている（4ファイル）
- その他の定義: 多数残っている（段階的に置換予定）

## 🎯 次のアクション（優先順位順）

1. **`placeholder:text-inverse/30`と`placeholder:text-inverse/40`の置換** 🟠
   - 使用箇所の確認（4ファイル）
   - `placeholder:text-[color-mix(...)]`への置換
   - `aliases.css`から定義を削除

2. **`aliases.css`の収束継続** 🟠
   - 他の定義の使用状況調査
   - 優先度の低い定義から段階的に置換
   - 広範囲に使用されている定義は後回し

3. **安全置換の実行** 🟡
   - codemod safe-pass の実行
   - 生色使用の継続削減

4. **scopes の実装** 🟢
   - 各スコープの実装と`data-scope`属性の追加

5. **v3ブリッジ削除計画** 🟢
   - 最終的な`aliases.css`削除に向けた計画

