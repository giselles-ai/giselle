# 次のタスク - 現在の状態と優先順位

## 🔴 緊急・ブロッカー

### 1. `bg-[color-mix(...)]`が効かない問題の解決
**現状:**
- `bg-[color-mix(...)]`がTailwindの任意値構文で正しく動作していない
- 一部のファイルで構文エラー（`in_srgb` → `in srgb`、`_5%` → ` 5%`）が残っている
- 67箇所で`bg-[color-mix(...)]`を使用中だが、動作していない

**調査が必要な原因:**
1. Tailwindの任意値構文が`color-mix()`を正しく処理していない可能性
2. CSS変数の解決タイミングの問題
3. ブラウザのサポートの問題
4. Tailwindの設定の問題

**TODO:**
- [ ] `bg-[color-mix(...)]`が効かない根本原因の特定
- [ ] 解決策の検討（`aliases.css`での定義、インラインスタイル、別のアプローチ）
- [ ] 構文エラーの修正（残っている箇所）
  - `account-display-name-form.tsx`
  - `dropdown-menu.tsx`
  - `password_reset/form.tsx`
  - `password_reset/new_password/form.tsx`
  - `form.tsx` (auth/components)
  - `login/form.tsx`
- [ ] 動作確認と視覚チェック

**影響範囲:**
- Settingsページの入力フィールド
- Authページの入力フィールド
- Workflow Designer UIの多数のコンポーネント
- UIコンポーネント（DropdownMenu、InviteMemberDialog、RoleMenu等）

## 🟠 高優先度

### 2. `border-inverse`の置換
**現状:**
- `border-inverse`が14箇所（10ファイル）で使用中
- `aliases.css`で定義されているが、`bg-inverse`と同様にトークン直参照へ置換が必要

**TODO:**
- [ ] `border-inverse`の使用箇所を調査
- [ ] `border-[color-mix(...)]`または`border-[var(--color-text-inverse)]`への置換
- [ ] `aliases.css`から`border-inverse`定義を削除

**影響ファイル:**
- `free-tag.tsx`
- `divider.tsx` (auth/components)
- `provider-button.tsx` (auth/components)
- `component.tsx` (text-editor)
- `components.tsx` (workflow-designer-ui outputs)
- `select.tsx` (ui/components)
- `model-picker.tsx`
- `aliases.css`

### 3. `aliases.css`の収束継続
**現状:**
- `text-link-muted`の置換完了
- `text-bg`の置換完了
- `bg-inverse`の置換完了（ただし動作していない）
- `border-inverse`が残っている

**TODO:**
- [ ] `border-inverse`の置換完了
- [ ] その他の`aliases.css`定義の使用状況調査
- [ ] 段階的な置換と削除

## 🟡 中優先度

### 4. 構文エラーの修正（残っている箇所）
**現状:**
- 9箇所で構文エラー（`in_srgb` → `in srgb`、`_5%` → ` 5%`）が残っている
- ただし、`bg-[color-mix(...)]`が動作していないため、修正しても動作しない可能性がある

**TODO:**
- [ ] 構文エラーの修正（`bg-[color-mix(...)]`問題解決後）
- [ ] 動作確認

**影響ファイル:**
- `account-display-name-form.tsx`
- `dropdown-menu.tsx`
- `password_reset/form.tsx`
- `password_reset/new_password/form.tsx`
- `form.tsx` (auth/components)
- `login/form.tsx`
- `prompt-editor.tsx`
- `generation-panel.tsx` (text-generation-node)
- `generate-cta-button.tsx`

### 5. 安全置換の実行
**現状:**
- `text-white-*/text-black-*`の置換完了（100%）
- `focus:ring-white/20`、`focus:ring-white/30`の置換完了
- `ring-white/10`の置換完了

**TODO:**
- [ ] codemod safe-pass の実行（dry-run完了、0件）
- [ ] 安全置換の第1弾実行（視覚差なし基準）
- [ ] 生色使用の継続削減（rgba/rgb/hex のトークン化）

## 🟢 低優先度

### 6. scopes の実装
**現状:**
- scopes の CSS ファイルは雛形のみ（空の状態）
- `data-scope`属性の使用が未実装

**TODO:**
- [ ] settings-apps スコープの実装と適用
- [ ] workspaces スコープの実装と適用
- [ ] stage スコープの実装と適用
- [ ] `data-scope`属性の追加と動作確認

### 7. コンポーネントの逆色対応
**現状:**
- `Separator`, `Tabs`は`variant="inverse"`対応済み
- `InverseSurface`は実装済みだが適用状況不明

**TODO:**
- [ ] `Switch/Slider`の逆色既定対応（未実装）
- [ ] `InverseSurface`の適用状況確認と拡大
- [ ] 逆色対応コンポーネントの統一確認

### 8. v3ブリッジ削除計画
**現状:**
- `aliases.css`の収束が進行中
- 互換トークン（`white-900`等）が残っている

**TODO:**
- [ ] v3ブリッジ段階削除計画の起票
- [ ] 互換トークン段階削除（`white-900`等）
- [ ] lintのwarn→errorへの格上げ（stylelint/ESLint）
- [ ] CI で生色/alias の残件を検出 → 増加fail化

## 📊 現在の統計

### `bg-[color-mix(...)]`の使用状況
- **総数:** 67箇所（44ファイル）
- **構文エラー:** 9箇所（7ファイル）

### `border-inverse`の使用状況
- **総数:** 14箇所（10ファイル）

### `aliases.css`の定義状況
- `text-link-muted`: ✅ 削除済み
- `text-bg`: ✅ 削除済み
- `bg-inverse`: ✅ 削除済み（ただし動作していない）
- `border-inverse`: ❌ 残っている

## 🎯 次のアクション（優先順位順）

1. **`bg-[color-mix(...)]`が効かない問題の解決** 🔴
   - 根本原因の特定
   - 解決策の検討と実装
   - 動作確認

2. **構文エラーの修正** 🔴
   - 残っている9箇所の修正
   - 動作確認

3. **`border-inverse`の置換** 🟠
   - 使用箇所の調査
   - トークン直参照への置換
   - `aliases.css`から削除

4. **`aliases.css`の収束継続** 🟠
   - 他の定義の使用状況調査
   - 段階的な置換と削除

5. **安全置換の実行** 🟡
   - codemod safe-pass の実行
   - 生色使用の継続削減

